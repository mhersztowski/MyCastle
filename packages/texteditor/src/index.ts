// @mhersztowski/texteditor — Monaco-based text/code editor stack.
// Depends only on @mhersztowski/core among MyCastle packages.

// vfs — VFS UI components + embedded project system
export * from './vfs';

// monaco — Monaco Editor wrapper (editor engine, plugin framework, AI agent, terminal)
export * from './monaco';

// mjd — MJD definition & data editors
export * from './mjd';

// plugins — built-in editor plugins
export * from './plugins';

// workspace — complete ready-to-use editor component (TextEditorWorkspace)
export * from './workspace';
