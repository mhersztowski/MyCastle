# URLs are intentionally left unset so the app auto-detects from window.location.host.
# This allows mobile devices on the same LAN to connect correctly (Vite proxy handles
# forwarding /api and /mqtt to the backend).
# VITE_MQTT_URL=ws://localhost:1894/mqtt
# VITE_HTTP_URL=http://localhost:1894
